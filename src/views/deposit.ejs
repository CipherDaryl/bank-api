<%- include('layout', { 
  title: 'Dépôt',
  user: user,
  message: message
}) %>
<div class="row justify-content-center">
  <div class="col-md-6">
    <div class="card">
      <div class="card-header bg-success text-white">
        <h4 class="mb-0"><i class="bi bi-plus-circle"></i> Dépôt</h4>
      </div>
      <div class="card-body">
        <form action="/deposit" method="POST">
          <input type="hidden" name="accountId" value="<%= accountId %>">
          <div class="mb-3">
            <label class="form-label">Montant (XAF) *</label>
            <input type="number" name="amount" class="form-control" required placeholder="0" min="1">
          </div>
          <div class="mb-3">
            <label class="form-label">Description</label>
            <input type="text" name="description" class="form-control" placeholder="Dépôt en espèces">
          </div>
          <div class="d-grid gap-2 d-md-flex justify-content-md-end">
            <a href="/dashboard" class="btn btn-secondary">Annuler</a>
            <button type="submit" class="btn btn-success">
              <i class="bi bi-plus-circle"></i> Déposer
            </button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>